node_modules/
dist/
.vite/
coverage/
.nyc_output/
.cache/
.eslintcache
*.log
npm-debug.log*

# Editor & VCS
.git/
.github/
.idea/
.vscode/
.DS_Store

# Local env
.env
.env.local
.env.*.local

# Tests are not needed at runtime; the multi-stage build runs `npm ci`
# in a builder stage anyway, so leaving them in COPY is fine — but
# excluding them keeps the build context smaller.
